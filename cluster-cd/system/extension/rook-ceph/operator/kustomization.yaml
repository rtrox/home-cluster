---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: rook-ceph

resources:
  - helm-release.yaml

configMapGenerator:
  - name: rook-values
    files:
      - values.yaml=values.yaml

configurations:
  - kustomizeconfig.yaml
