---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring

resources:
  - helm-release.yaml
  - thanos-objstore-secret.x86.yaml

configMapGenerator:
  - name: kube-prometheus-stack-values.yaml
    files:
      - values.yaml=values.yaml

configurations:
  - kustomizeconfig.yaml
