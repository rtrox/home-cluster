---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: velero

resources:
  - helm-release.yaml
  - servicemonitor.yaml

configMapGenerator:
  - name: velero-pvc-watcher-helm-values
    files:
      - values.yaml=helm-values.yaml

configurations:
  - kustomizeconfig.yaml
