---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: authentik

resources:
  - authentik-chart.yaml

secretGenerator:
  - name: authentik-values
    files:
      - values.yaml=authentik-values.cho-raw.yaml

configurations:
  - kustomizeconfig.yaml
