---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Pre Flux-Kustomizations
  # - ./namespace.yaml # Currently conflicts as this was moved. re-enable when deleted elsewhere.
  # Flux-Kustomizations
  # - ./zwavejs/ks.yaml
