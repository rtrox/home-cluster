---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: &name fileshelter-data
  labels:
    app.kubernetes.io/name: *name
    app.kubernetes.io/instance: fileshelter
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
