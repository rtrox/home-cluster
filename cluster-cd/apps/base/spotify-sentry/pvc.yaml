---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: &name spotify-sentry-data
  labels:
    app.kubernetes.io/name: *name
    app.kubernetes.io/instance: spotify-sentry
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
