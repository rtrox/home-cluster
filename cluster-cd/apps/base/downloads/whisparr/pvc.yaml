---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: &name whisparr-config
  labels:
    app.kubernetes.io/name: *name
    app.kubernetes.io/instance: whisparr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi
