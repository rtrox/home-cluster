---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: &name prowlarr-config
  labels:
    app.kubernetes.io/name: *name
    app.kubernetes.io/instance: prowlarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
