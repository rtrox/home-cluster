---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: &name quickshare-data
  labels:
    app.kubernetes.io/name: *name
    app.kubernetes.io/instance: quickshare
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
