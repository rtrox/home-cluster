---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - statefulset.yaml
  - service.yaml

configMapGenerator:
  - name: redis-config
    namespace: blocky
    files:
      - redis.conf=redis.conf

images:
  - name: redis
    newTag: 7.0.12-alpine

commonLabels:
  app.kubernetes.io/part-of: blocky
  k8s-app: blocky
