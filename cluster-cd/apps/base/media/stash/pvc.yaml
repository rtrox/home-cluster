---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: &name stash-config
  labels:
    app.kubernetes.io/name: *name
    app.kubernetes.io/instance: stash
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
