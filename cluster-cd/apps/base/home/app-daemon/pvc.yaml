---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: &name app-daemon-data
  labels:
    app.kubernetes.io/name: *name
    app.kubernetes.io/instance: app-daemon
spec:
  storageClassName: synology-iscsi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
