---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: home

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - code-ingress.yaml
  - pvc.yaml

configMapGenerator:
  - name: appdaemon-yaml
    files:
      - appdaemon.yaml=appdaemon.yaml

images:
  - name: acockburn/appdaemon
    newTag: "4.2.1"
