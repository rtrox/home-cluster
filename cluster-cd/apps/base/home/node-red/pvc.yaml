---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: &name node-red-data
  labels:
    app.kubernetes.io/name: *name
    app.kubernetes.io/instance: node-red
spec:
  storageClassName: synology-iscsi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
