---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: home

resources:
  - helm-release.yaml

configMapGenerator:
  - name: emqx-helm-values
    files:
      - values.yaml=helm-values.yaml

secretGenerator:
  - name: emqx-helm-values-secret
    files:
      - values.yaml=helm-values-secret.x86-raw.yaml

configurations:
  - kustomizeconfig.yaml
