---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: &name heimdall-config
  labels:
    app.kubernetes.io/name: *name
    app.kubernetes.io/instance: heimdall
spec:
  storageClassName: synology-iscsi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
