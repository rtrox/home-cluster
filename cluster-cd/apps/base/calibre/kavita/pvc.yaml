---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: &name kavita-config
  labels:
    app.kubernetes.io/name: *name
    app.kubernetes.io/instance: kavita
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
