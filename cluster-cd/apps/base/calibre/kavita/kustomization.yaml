---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - pvc.yaml

images:
  - name: kizaing/kavita
    newTag: 0.6.1

commonLabels:
  app.kubernetes.io/part-of: kavita
  k8s-app: kavita
